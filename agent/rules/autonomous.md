---
applyTo: "**"
---

# Autonomous session rules

These rules apply to Claude Code sessions running autonomously via StarLoop.

## Pull requests

After raising or updating a PR (pushing new commits to an existing PR branch), post a comment tagging @homostellaris so they receive a push notification:

```
gh pr comment <number> --body "@homostellaris PR ready for review."
```
